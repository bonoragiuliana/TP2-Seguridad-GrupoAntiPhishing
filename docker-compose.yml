version: "3.9"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    command: node src/server.js
    env_file:
      - .env



